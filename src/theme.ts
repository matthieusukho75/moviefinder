import { createTheme } from "@mui/material";

export const colors = {
  softBlue: "#586E94",
  hardBlue: "#253755",
};

export const theme = createTheme({
  palette: {},
  typography: {
      
    body1: {
      color: "#FFFF",
    },
    h6: {
      color: "#FFFF",
      fontWeight: 700,
      fontSize: "26px",
      lineHeight: "30.47px",
    },
  },
});
